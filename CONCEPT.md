# 1. Python 기초
## 1.1 변수
- 변수: 값을 저장하고 관리하기 위한 상자

- 변수를 사용하는 이유
    1. **데이터 재사용**: 한 번 저장해둔 값을 여러 번 반복해서 편하게 불러올 수 있음
    2. **가독성 향상**: 숫자,문자 대신 의미있는 이름을 사용하면 코드를 이해하기 쉬워짐
    3. **유지보수 용이**: 코드 속의 값을 변경할 일이 있을 때 변수 정의 부분만 바꾸면 됨
    

## 1.2 자료형
| 자료형 | 이름 | 설명 | 주요 특징 | 작성 예시 |
| :--- | :--- | :--- | :--- | :--- |
| **`int`** | 정수형 | 소수점이 없는 숫자 데이터 | 사칙연산 등 산술 계산 가능 | `age = 25` |
| **`str`** | 문자열 | 따옴표로 감싼 텍스트 데이터 | 문자 이어붙이기 및 텍스트 조작 가능 | `name = "파이썬"` |
| **`bool`** | 불리언 | 참/거짓 판단용 데이터 | `True` 또는 `False` 값만 포함 | `is_active = True` |
| **`list`** | 리스트 | 순서가 있는 데이터의 목록 | 값 수정/추가/삭제 가능, 인덱스로 접근 | `items = ["사과", "바나나"]` |
| **`dict`** | 딕셔너리 | Key-Value 짝을 이루는 데이터 | 키(Key) 이름으로 빠르게 값 검색 가능 | `profile = {"age": 20}` |

- list
    - 메모리에 연속적으로 저장
    - 데이터를 순서대로 열거하거나 중복 허용하는 목록 필요할 때 효율적

 - dict
    - 데이터를 찾아야하는 상황에서는 dict가 효율적
    - 해시 기반 -> key 수정 불가


## 1.3 for VS while
| 구분 | for문 | while문 |
| :--- | :--- | :--- |
| **핵심 개념** | **반복 횟수가 정해져 있거나** 컬렉션을 순회할 때 사용 | **특정 조건이 참(True)인 동안** 지속적으로 반복 |
| **주요 목적** | 정해진 범위, 리스트, 튜플 등의 원소를 순차적으로 처리 | 조건식의 결과에 따라 반복 여부를 결정 |
| **반복 종료 조건** | 시퀀스의 모든 원소를 소비하거나 지정된 범위를 마쳤을 때 | 조건식이 `False`가 되거나 내부에서 `break`를 만났을 때 |
| **무한 루프 위험** | 비교적 낮음 (지정된 범위 내에서만 작동) | 조건 업데이트 누락 시 **무한 루프(Infinite Loop)** 위험 존재 |
| **주요 활용 상황** | 리스트/문자열 탐색, 정해진 횟수($N$번)만큼 반복 수행 | 조건 만족 시까지 대기, 무한 반복 게임/서버 로직, 사용자 입력 수신 |

- for문: 반복할 횟수,대상이 명확한 경우
- while문: 특정 조건을 만족하는 동안 계속 실행해야 하는 경우


# 2. 클래스와 객체
## 2.1 클래스
| 구분 | 설명 | 비유 |
| :--- | :--- | :--- |
| **클래스 (Class)** | 객체(Object)를 만들기 위한 **설계도** 또는 **틀** | 붕어빵 틀, 자동차 설계도 |
| **객체 (Object) / 인스턴스 (Instance)** | 클래스를 바탕으로 **실제 메모리에 생성된 결과물** | 실제 구워진 붕어빵, 생산된 자동차 |

- 클래스: 연관된 데이터(변수/속성)와 그 데이터를 다루는 기능(함수/메서드)을 하나로 묶은 틀
- 객체(인스턴스): 클래스를 바탕으로 실제 메모리에 생성된 결과물

### 클래스를 사용하는 이유
1. **코드의 재사용성 및 생산성 향상**
    - 동일한 구조 반복 대신 틀 기반으로 객체 계속 찍어낼 수 있음
    - 코드 중복 제거, 일관된 구현
2. **독립된 상태 관리**
    - 각 객체가 자신만의 데이터(속성)를 유지
    - 다른 객체에 영향 없이 독립적으로 동작
3. **코드의 유지보수 용이(캡슐화)**
    - 관련된 변수와 함수를 하나로 묶어 관리
    - 내부 로직 수정해도 코드 외부로 미치는 영향 최소화
4. **상속 및 확장성**
    - 기존 클래스의 기능을 상속받아 새로운 클래스 생성
    - 코드 재사용 및 기능 확장


## 2.2 __init__ 메서드

| 구분 | `__init__` 메서드 | `self` 키워드 |
| :--- | :--- | :--- |
| **개념** | 클래스의 **생성자(Constructor)** 메서드 | 생성된 **객체(인스턴스) 자기 자신**을 가리키는 변수 |
| **역할** | 객체가 생성될 때 **속성(변수)을 초기화** | 객체 내부의 속성 및 메서드에 접근할 때 사용 |
| **호출 시점** | 객체 생성 시 (`Class()`) **자동으로 실행** | 메서드 호출 시 파이썬이 첫 번째 인자로 **자동 전달** |


### `__init__` 메서드
* **정의:** 객체가 만들어질 때 가장 먼저 자동으로 호출되는 특별한 메서드입니다.
* **목적:** 객체가 가질 초기 데이터(속성)를 설정합니다.

### `self`
* **정의:** 현재 작업 중인 **"객체 자기 자신"**을 의미합니다.
* **목적:** 클래스라는 틀에서 만들어진 여러 객체들이 서로의 데이터를 섞이지 않고 **독립적으로 관리**할 수 있게 구분해 주는 주소표 역할을 합니다.
* **특징:** 메서드를 정의할 때 첫 번째 매개변수로 반드시 써주어야 하지만, **호출할 때는 직접 인자를 넘기지 않습니다.** (파이썬이 알아서 넘겨줍니다.)


## 2.3 == VS is
- `==` (동등 비교, Equility)
    - 두 객체의 '값'이 같은지 비교합니다.
    - 숫자, 문자열, 리스트 등 내용물을 비교할 때 사용합니다.

- `is` (동일성 비교, Identity)
    - 두 객체가 메모리상에서 '동일한 주소(객체)'를 가리키는지 비교합니다.
    - `a = list1` 처럼 할당했을 때만 `True`가 됩니다.
    - `id(a) == id(b)`와 동일한 의미로 사용됨

- 왜 `is`를 쓸까?
    - 1. 싱글톤(Singleton) 객체 비교 (val_range is not None)
        - 파이썬에서 None은 메모리상 단 하나의 객체만 존재
        - `== None` 보다 `is not None`이 더 빠름

## 2.4 @classmethod
- **클래스 메서드(Class Method)**: 인스턴스(객체)가 아닌 클래스 자체에 속하며 클래스 상태를 조작할 때 사용하는 메서드입니다.

### 1) 주요 특징
* **첫 번째 매개변수 `cls`**: 일반 메서드가 객체 자기 자신을 가리키는 `self`를 첫 번째 인자로 받는 것과 달리, 클래스 메서드는 클래스 자체를 가리키는 `cls`를 첫 번째 인자로 받습니다.
* **클래스 이름을 통한 호출**: 개별 인스턴스 객체를 생성하지 않고도 `클래스이름.메서드이름()` 형식으로 클래스 이름을 통해 바로 호출할 수 있습니다.
* **동적 인스턴스 생성**: `cls`를 함수처럼 호출하여 해당 클래스의 새로운 인스턴스를 동적으로 생성하고 반환할 수 있습니다.

### 2) 주요 용도: 팩토리 메서드 (Factory Method)
* 다양한 형태의 입력 데이터를 받아서 필요한 인스턴스 객체를 조립하고 복원하는 또 다른 생성자 역할을 수행할 때 사용됩니다.
* **우리 코드에서의 예시**:
  * [`Quiz.from_dict(cls, data)`](file:///Users/ys/Desktop/STUDY/Codyssey/codyssey-e1-2-python/Quiz/quiz_game.py#L36-L39) 메서드는 JSON에서 로드한 딕셔너리 데이터를 전달받아 `Quiz` 객체로 직접 변환 및 생성해 줍니다.
  ```python
  @classmethod
  def from_dict(cls, data: dict):
      # cls는 Quiz 클래스를 가리키며, 아래 코드는 Quiz(data["question"], ...)와 동일하게 동작합니다.
      return cls(data["question"], data["choices"], data["answer"])
  ```
from_dict는 "객체를 새로 만드는 것"이 목적입니다. 딕셔너리 데이터를 받아와서 퀴즈 객체를 탄생시켜야 하는 단계이므로, 호출하는 시점에는 아직 객체(self)가 존재하지 않음

# 3. 파일 입출력
## 3.1 with open(self.filepath, "r", encoding="utf-8") as f:
- with
    - 파일을 열고 블록을 벗어나면 자동으로 파일이 닫히도록 하는 구문
    - 파일이 제대로 닫히지 않아 발생하는 문제 방지
    - f.close()를 하지 않아도 됨 -> 무조건 파일을 닫아줌으로써 발생하는 오류 방지

- 자동 종료 보장: 블록 안의 코드가 정상적으로 끝나거나, 에러 발생 여부와 상관없이 파일을 '안전하게 닫아줌'
    - 이를 통해 파일 리소스 누수(Resource Leak)를 원천적으로 방지하고 코드 안정성을 크게 높일 수 있음.
        - 리소스 누수: 함수가 종료가 되었지만. OS 입장에서 여전히 "파일 사용 중"으로 인식 → 시스템 리소스가 부족해질 수 있음



## 3.2 UTF-8
- 전 세계 모든 문자를 표현할 수 있는 인코딩 방식
- 한글이 포함된 텍스트 파일을 불러오거나 저장할 때 반드시 필요
- 1~4바이트로 가변 길이


## 4 JSON
- **JSON(JavaScript Object Notation)**: 자바스크립트 객체 표기법에서 유래한 텍스트 기반의 데이터 교환 포맷

### 4.1 JSON의 핵심 특징
* **가독성과 경량성**: 텍스트 기반으로 이루어져 있어 사람이 읽고 쓰기 쉬우며, 컴퓨터가 파싱(분석)하기에 매우 가볍고 빠릅니다.
* **언어 독립성**: 특정 프로그래밍 언어에 종속되지 않아, Python, JavaScript, Java, Go 등 대부분의 언어에서 표준 데이터 교환 수단으로 사용됩니다.
* **구조적 데이터 표현**: 두 가지 구조로 모든 데이터를 표현합니다.
  * **객체(Object)**: 중괄호 `{}` 안에 `Key-Value` 쌍을 담는 구조 (Python의 `dict`에 매핑)
  * **배열(Array)**: 대괄호 `[]` 안에 값의 목록을 담는 순서 있는 구조 (Python의 `list`에 매핑)
* **지원하는 데이터 타입**:
  * 문자열(`"text"`), 숫자(`123`, `3.14`), 불리언(`true`, `false`), 객체(`{}`), 배열(`[]`), 빈 값(`null`)
  * *참고*: 파이썬의 `True`, `False`, `None`은 JSON으로 직렬화되면 소문자 `true`, `false`, `null`로 자동 변환됩니다.

### 4.2 다른 파일 형식이 아닌 JSON을 쓰는 이유
* **딕셔너리/리스트와 1:1 매핑**: 파이썬의 중첩된 딕셔너리와 리스트 구조가 JSON의 객체 및 배열 구조와 일치하여 변환(`json.dump`, `json.load`)이 매우 직관적이고 간단합니다.
* **XML, CSV와의 차이점**:
  * **CSV**: 구조가 단순하여 2차원 표 형식(테이블) 데이터만 다루기 쉬우나, 중첩 구조나 복잡한 데이터를 저장하기는 한계가 있습니다.
  * **XML**: 닫는 태그(`</tag>`)가 많아 JSON에 비해 가독성이 떨어지고 파일 크기가 크며 파싱 속도가 느립니다.
  * **JSON**: 복잡하고 계층적인 데이터 구조를 구조적으로 저장하기에 가장 균형 잡힌 포맷입니다.


## 5. Git 명령어
- init: 깃 저장소 초기화
- add: 로컬 저장소에 파일 추가
- commit: 로컬 저장소에 파일 커밋
- push: 로컬 저장소 -> 원격 저장소
- pull: 원격 저장소 -> 로컬 저장소
- checkout: 특정 브랜치로 이동
- clone: 원격 저장소 복제



